# 对栈的认识

## 我对栈的理解

我觉得栈其实就是单链表的头插法，但有点不同，不同的地方不是数据的**存放**，而是对数据的**使用**。单链表可以对表里的所有数据都有一遍历练，而是对数据的**使用**。单链表可以从表中修改和使用数据，而栈不行，它必须是对**最后一个**放入的数据进行使用，就像羽毛球桶一样，每一次只能先拿出后放入的羽毛球，毕竟先放入的球在下面嘛。
##我的问题

- 数据结构指的是什么？是对数据的存放还是对数据的使用方式
- 为什么说表、栈、队是三大基础数据结构？
-递归需要用到栈，确实，需要将每一次的结果进行储存，就需要压入栈里，但如果是void型函数递归呢？
